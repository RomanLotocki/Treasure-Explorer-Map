{% extends 'base.html.twig' %}

{% block title %}Mofifier utilisateur{% endblock %}

{% block body %}
    <h1>Modifier le compte de {{ user.firstName }} {{ user.lastName }}</h1>

    {{ include('user/_form.html.twig', {'button_label': 'modifier'}) }}

    <a class="btn btn-primary my-2" href="{{ path('app_user_index') }}">retour à la liste</a>

    {{ include('user/_delete_form.html.twig') }}
{% endblock %}
