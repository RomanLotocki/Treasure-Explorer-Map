{% extends 'base.html.twig' %}

{% block title %}Modifier l'objet{% endblock %}

{% block body %}
    <h1>modifier {{ item.itemName }}</h1>

    {{ include('item/_form.html.twig', {'button_label': 'modifier'}) }}

    <a class="btn btn-primary my-2" href="{{ path('app_item_index') }}">retour à la liste</a>

    {{ include('item/_delete_form.html.twig') }}
{% endblock %}
