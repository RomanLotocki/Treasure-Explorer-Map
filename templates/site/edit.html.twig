{% extends 'base.html.twig' %}

{% block title %}Modifier le site{% endblock %}

{% block body %}
    <h1>modifier {{ site.name}}</h1>

    {{ include('site/_form.html.twig', {'button_label': 'modifier'}) }}

    <a class="btn btn-primary my-2" href="{{ path('app_site_index') }}">retour à la liste</a>

    {{ include('site/_delete_form.html.twig') }}
{% endblock %}
